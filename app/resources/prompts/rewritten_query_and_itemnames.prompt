请根据历史会话和当前问题，提取用户正在询问的出海业务知识主体（item_names）。
说明：当前代码字段名仍叫 item_names，但业务含义是 knowledge entities，可以是产品线、国家/市场、政策、SOP、运营场景、客服场景、合规主题或版本说明。

1. 如果用户明确提到了业务主体，请提取出来。可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个"、"它"、"该市场"、"这个政策"），请结合历史会话指代消解，确定业务主体。
3. 如果无法确定业务主体，item_names 返回空列表。
4. 请重新改写用户的问题（rewritten_query），使其成为包含业务主体的独立完整问题，注意不要超过120个字符。
5. 本次提问和之前的主体不一定连续，所以以当前用户问题为主，以历史会话为辅进行识别。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["业务主体A", "业务主体B"],
    "rewritten_query": "关于业务主体A和业务主体B，..."
}}

当前用户问题：{query}

历史会话：
{history_text}
